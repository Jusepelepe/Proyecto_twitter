<div class="row">
  <% if user_signed_in? %>
    <div class="col-12">
      <%= render 'tweets/form', tweet: @tweet %>
    <% end %>
  </div>



  <%= render 'shared/tweet_card', tweet: @tweet %>

  <%= paginate @tweets %>
</div>
